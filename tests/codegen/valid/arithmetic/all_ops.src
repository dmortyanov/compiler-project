// Test: all arithmetic operations
fn compute(int a, int b) -> int {
    int sum = a + b;
    int diff = a - b;
    int prod = a * b;
    int quot = a / b;
    int rem = a % b;
    return sum + diff + prod + quot + rem;
}

fn main() -> int {
    return compute(10, 3);
}
