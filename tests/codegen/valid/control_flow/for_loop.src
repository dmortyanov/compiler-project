// Test: for loop (Sprint 6 LOOP-2)
// Computes sum of squares: 1^2 + 2^2 + 3^2 + 4^2 = 1 + 4 + 9 + 16 = 30
fn main() -> int {
    int sum = 0;
    int i = 1;
    while (i <= 4) {
        sum = sum + i * i;
        i = i + 1;
    }
    return sum;
}
